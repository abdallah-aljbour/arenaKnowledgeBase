import React from "react";
import "../styles/sidebar.scss";

const Sidebar: React.FC = () => {
  return (
    <div className="sidebar">
      {/* Sidebar container, no items or submenus for now */}
    </div>
  );
};

export default Sidebar;
